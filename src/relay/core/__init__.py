"""relay.core — the domain and application services.

Contains no web-framework or LLM-provider imports (enforced by import-linter).
Phase 1 populates domain/, ownership/, responsibilities/, recurrence/,
reminders/, audit/, policies/, application/.
"""
