"""relay.ai — bounded compiler from free text to a draft Responsibility Graph.

Never assigns ownership, accepts transfers, or touches reminders. Provider code
lives here, outside relay.core. Phase 9 populates providers/, schemas/,
extraction/, validation/, provenance/, fallback/, evals/.
"""
